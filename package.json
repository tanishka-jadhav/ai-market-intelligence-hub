{
  "name": "ai-market-intelligence-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm install && npm run build",
    "start": "cd apps/web && npm run start"
  }
}
